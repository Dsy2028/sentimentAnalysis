have python and nodejs on installed machine
$ cd app
$ npm install
$ npm run dev
in vscode  ctrl shift p or => view => command pallete => python : create environment = > venv
open new terminal
$ python -m pip install flask and pip install Flask-Cors
$ python app.py
